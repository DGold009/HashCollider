# HashCollider has NO third-party runtime dependencies.
# It uses only the Python standard library (hashlib, secrets, argparse, json, ...).
#
# This file exists so that `pip install -r requirements.txt` works in the
# documented installation steps. Install the package itself with:
#
#     pip install -e .
